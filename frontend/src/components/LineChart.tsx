import { LineChart as Chart, Line, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
export function LiveChart({title,data,dataKey,color}:{title:string;data:Record<string,unknown>[];dataKey:string;color:string}) {return <article className="card chart"><h2>{title}</h2><ResponsiveContainer width="100%" height={190}><Chart data={data}><XAxis dataKey="time" tick={{fill:'#8291ad',fontSize:11}}/><YAxis tick={{fill:'#8291ad',fontSize:11}} domain={['auto','auto']}/><Tooltip/><Line type="monotone" dataKey={dataKey} stroke={color} strokeWidth={2} dot={false} isAnimationActive/></Chart></ResponsiveContainer></article>}
